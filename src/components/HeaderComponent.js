import React from 'react';
import {headerContainer} from '../assets/Header.module.scss';
const HeaderComponent = () => {
    return (
        <div className={headerContainer}>
            <h2>
                TODO LIST REACTJS 2022</h2>
        </div>
    );
};

export default HeaderComponent;
